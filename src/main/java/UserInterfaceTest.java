import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.when;


class UserInterfaceTest {
/*
    @Mock
    private FileHandler mockFiler;

    @Mock
    private CipherSimple mockCipher;

    @InjectMocks
    private CmdLineUtil cmdLineUtil;

    @Test
    void testZeroInput() {
        when(mockFiler.readFileList()).thenReturn("1. File1");

    }
    @Test
    void testOneInput() {
        when(mockFiler.read(0)).thenReturn("obtained file");
        when(mockCipher.applyCipher()).thenReturn("Contents of the file using default cipher")

    }
    @Test
    void testTwoInput() {
        when(mockFiler.read(0)).thenReturn("obtained file");
        when(mockCipher.applyCipher(Cipher)).thenReturn("Contents of the file using provided cipher")
    }
    */
}

