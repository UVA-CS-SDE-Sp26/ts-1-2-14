filehandler.txt
--------------------------------------------------------------------
-String folderPath
--------------------------------------------------------------------
-isValidRequest(): Boolean
+getFileContents(String file_name): String


Tests:
Assume data folder path is in the root of the project folder
Assume the current folder only contains “test.txt”

//test error handling when an invalid file_name is given
Call getFileContents(invalid.txt)
Check if invalid.txt is a valid file name
If yes:
     Print the contents of invalid.txt
Else:
     Print an error msg and return nothing

Expected Behavior - should print and error message and return nothing

//test expected behavior
Call getFileContents(test.txt)
Check if test.txt is a valid file name
If yes:
     Print the contents of test.txt
Else:
     Print an error msg and return nothing

Expected Behavior - should correctly print the contents of test.txt
